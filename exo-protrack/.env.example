# ============================================
# EXO ProTrack Environment Configuration
# ============================================
# Copy this file to .env.local for local development
# or configure these variables in your deployment platform
# ============================================

# --------------------------------------------
# Supabase Configuration (REQUIRED)
# --------------------------------------------
# Get these from your Supabase Project Settings > API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# --------------------------------------------
# Server-side only (REQUIRED for /api/seed endpoint)
# --------------------------------------------
# NEVER expose these to the client!
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# SUPABASE_ANON_KEY=your-anon-key-here
# SUPABASE_DB_PASSWORD=your-db-password

# --------------------------------------------
# Seed API Authentication (REQUIRED for /api/seed)
# --------------------------------------------
# Secret key for authenticating requests to the seed endpoint.
# Pass via Authorization header: "Bearer <SEED_API_SECRET>"
# SEED_API_SECRET=your-secure-random-secret

# --------------------------------------------
# Feature Flags (Optional)
# --------------------------------------------
VITE_ENABLE_MOCK_DATA=false
VITE_DEBUG_MODE=false
